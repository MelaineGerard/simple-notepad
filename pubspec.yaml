name: simple_notepad
description: A new Flutter project.
publish_to: 'none'

version: 0.1.3+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  file_picker: ^10.3.10
  flutter:
    sdk: flutter
  handy_window: ^0.4.2
  pdf: ^3.11.3
  printing: ^5.11.0
  shared_preferences: ^2.5.4
  yaru: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true
  assets: []

